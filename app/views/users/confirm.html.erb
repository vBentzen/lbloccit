<h2>Sign Up Confirmation</h2>





<div class="row">
  <div class="col-md-8">
    <div class="form-group">
      <h3>Are you sure these vallues are correct?</h3>
      <p><%= "Email: #{@user.email}" %></p>
      <p><%= "Name: #{@user.name}" %></p>
    </div>
    <div class="form-group">
      <%= button_to "Yes", {controller: "users", action: "create", params: params}, class: 'btn btn-success' %>
      <%= button_to "No", {controller: "users", action: "new" }, class: 'btn btn-danger' %>
    </div>
    </div>
</div>